import Footer from "@/common/components/ui/footer/footer.component";

export default {
    title: "Components/Footer",
    component: Footer
}

const Template = args => <Footer {...args} />

export const Default = Template.bind({})
Default.args = {
    
}